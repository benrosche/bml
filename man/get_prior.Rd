% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/priors.R
\name{get_prior}
\alias{get_prior}
\title{List every settable prior of a bml model}
\usage{
get_prior(formula, data, family = stats::gaussian())
}
\arguments{
\item{formula}{The model formula (same as for \code{\link{bml}}).}

\item{data}{The data (member-level long format).}

\item{family}{Model family (function, object, or string). Default \code{gaussian()}.}
}
\value{
A data frame with columns \code{class}, \code{coef}, \code{group},
  \code{node} (the internal JAGS node), and \code{default}.
}
\description{
Returns the table of all parameters whose priors can be set via
\code{\link{prior}}, together with their default priors — the bml analogue of
\code{brms::get_prior()}. Use it to discover valid \code{class}/\code{coef}/
\code{group} combinations before overriding.
}
\examples{
\dontrun{
get_prior(
  y ~ x + mm(id = id(pid, gid), vars = vars(z), w = w(~ 1/n)),
  data = dat, family = gaussian()
)
}

}
\seealso{
\code{\link{prior}}, \code{\link{bml}}
}
