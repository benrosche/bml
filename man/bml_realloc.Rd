% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/temporal.R
\name{bml_realloc}
\alias{bml_realloc}
\title{Reallocation of membership weights toward high-attribute members}
\usage{
bml_realloc(data, group, member, time, weight = NULL, x, t1, t2)
}
\arguments{
\item{data}{Member-level long data covering both snapshots.}

\item{group, member, time}{Column names (strings): group id, member id, and
the time variable.}

\item{weight}{Column name (string) of the raw weight, or \code{NULL} for
equal weights.}

\item{x}{Column name (string) of the member attribute.}

\item{t1, t2}{The two time values to compare.}
}
\value{
A data frame with one row per group: \code{realloc_x}
  (\eqn{\sum_k \Delta w_k x_k}; reallocation toward high-\code{x} members)
  and \code{realloc_intensity} (\eqn{\sum_k (\Delta w_k)^2}; how much
  reshuffling happened, regardless of direction).
}
\description{
The headline temporal micro-macro term: did the group shift weight mass
toward members with high \eqn{x}? Computes \eqn{\sum_k \Delta w_k x_k} over
the union roster (using each member's observed attribute) plus the
reallocation \emph{intensity} \eqn{\sum_k \bar w_k (\Delta w_k)^2}-style
spread measures.
}
\seealso{
\code{\link{bml_delta}}, \code{\link{bml_turnover}}
}
